services:
  meilisearch-test:
    image: getmeili/meilisearch:v1.50@sha256:9694a59df43ee3f54b3fda9c5de381a3ee9852678e3e31cadf37d6bddea7fc1b
    restart: unless-stopped
    container_name: ddev-${DDEV_SITENAME}-meilisearch-test
    hostname: ${DDEV_SITENAME}-meilisearch-test
    labels:
      com.ddev.site-name: ${DDEV_SITENAME}
      com.ddev.approot: $DDEV_APPROOT
    environment:
      - MEILI_MASTER_KEY=dev-master-key-change-in-production
      - MEILI_ENV=development
      - MEILI_HTTP_ADDR=0.0.0.0:7700
      - MEILI_LOG_LEVEL=WARN
    networks:
      - ddev_default
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:7700/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
