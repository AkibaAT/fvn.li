x-fvnli: &fvnli
  image: ${DOCKER_IMAGE}
x-db: &db
  image: postgres:18.4@sha256:775a0820dd34e907ec63277e388d145056d427548e461d7953a070edb5de3a3a
x-meilisearch: &meilisearch
  image: getmeili/meilisearch:v1.49@sha256:bdc7d7e7939911c40d88d6bcd01f9c72c81f7293135916d48bce241569f721bd
x-redis: &redis
  image: redis:8.8.0@sha256:2838d5524559494f6f1cd66e97e76b200d64a633a8614200620755ed395daf32
x-denkit-stash: &denkit-stash
  image: ${DENKIT_STASH_IMAGE}
x-minio: &minio
  image: ${MINIO_DOCKER_IMAGE:-minio/minio:latest}

services:
  app:
    <<: *fvnli
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      meilisearch:
        condition: service_healthy
      flaresolverr:
        condition: service_healthy
      stats-runner:
        condition: service_started
      denkit-stash:
        condition: service_healthy
    environment:
      - RENPY_ANALYSIS_MODE=${RENPY_ANALYSIS_MODE:-sandbox}
      - RENPY_ANALYZER_URL=${RENPY_ANALYZER_URL:-http://stats-runner:8080/api/renpy-analyzer/analyze}
      - RENPY_ANALYZER_TOKEN=${RENPY_ANALYZER_TOKEN}
      - RENPY_ANALYZER_TIMEOUT=${RENPY_ANALYZER_TIMEOUT:-300}
      - FLARESOLVERR_URL=${FLARESOLVERR_URL:-http://flaresolverr:8191}
      - DENKIT_STASH_URL=${DENKIT_STASH_URL:-http://denkit-stash:8081}
      - DENKIT_STASH_PUBLIC_URL=${DENKIT_STASH_PUBLIC_URL:-https://denkit-stash.${APP_DOMAIN}}
      - DENKIT_STASH_USERNAME=${DENKIT_STASH_USERNAME:-fvn-li}
      - DENKIT_STASH_API_KEY=${DENKIT_STASH_API_KEY}
      - BUTLER_CLIENT_PATH=${BUTLER_CLIENT_PATH:-/opt/butler}
      - ARCHIVE_OPTIMIZER_IMAGE=${DOCKER_IMAGE}
      - ARCHIVE_OPTIMIZER_HOST_WORK_DIR=/var/lib/fvn-archive-optimizer/${COMPOSE_PROJECT_NAME}
      - ARCHIVE_OPTIMIZER_CONTAINER_WORK_DIR=/optimizer-work
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost/health" ]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      fvnli:
        aliases:
          - laravel-app
      flaresolverr:
      web:
    volumes:
      - app_storage:/app/storage
      - caddy_data:/data
      - caddy_config:/config
      - /opt/renpy-sdk:/opt/renpy-sdk:ro
      - /var/lib/fvn-archive-optimizer/${COMPOSE_PROJECT_NAME}:/optimizer-work
      - /var/log/fvn/${COMPOSE_PROJECT_NAME}:/app/storage/logs
      - /var/run/docker.sock:/var/run/docker.sock
      - ./:/app/
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=web"
      - "traefik.http.routers.${COMPOSE_PROJECT_NAME}.rule=Host(`${APP_DOMAIN}`) || Host(`www.${APP_DOMAIN}`)"
      - "traefik.http.services.${COMPOSE_PROJECT_NAME}.loadbalancer.server.port=80"
      - "traefik.http.routers.${COMPOSE_PROJECT_NAME}.tls=true"
      - "traefik.http.routers.${COMPOSE_PROJECT_NAME}.entrypoints=https"
  stats-runner:
    <<: *fvnli
    restart: unless-stopped
    working_dir: /app/public
    entrypoint:
      - php
      - -S
      - 0.0.0.0:8080
      - ../vendor/laravel/framework/src/Illuminate/Foundation/resources/server.php
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8080/health" ]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - fvnli
    environment:
      - APP_CONFIG_CACHE=/app/bootstrap/cache/config-stats-runner.php
      - RENPY_ANALYZER_SERVER=true
      - RENPY_ANALYZER_TOKEN=${RENPY_ANALYZER_TOKEN}
      - RENPY_ANALYZER_IMAGE=${DOCKER_IMAGE}
      - RENPY_ANALYZER_TIMEOUT=${RENPY_ANALYZER_TIMEOUT:-300}
      - RENPY_ANALYZER_HOST_WORK_DIR=/var/lib/fvn-renpy-analyzer/${COMPOSE_PROJECT_NAME}
      - RENPY_ANALYZER_CONTAINER_WORK_DIR=/runner-work
      - RENPY_SDK_DOCKER_HOST_PATH=${RENPY_SDK_DOCKER_HOST_PATH:-/opt/renpy-sdk}
      - RENPY_SDK_PATH=/opt/renpy-sdk
    volumes:
      - app_storage:/app/storage
      - /opt/renpy-sdk:/opt/renpy-sdk:ro
      - /var/lib/fvn-renpy-analyzer/${COMPOSE_PROJECT_NAME}:/runner-work
      - /var/log/fvn/${COMPOSE_PROJECT_NAME}:/app/storage/logs
      - /var/run/docker.sock:/var/run/docker.sock
      - ./:/app/
  denkit-stash:
    <<: *denkit-stash
    restart: unless-stopped
    command: ["./denkit-stash", "--port=8081"]
    depends_on:
      db:
        condition: service_healthy
      minio:
        condition: service_healthy
    healthcheck:
      test: [ "CMD-SHELL", "wget --no-verbose --tries=1 --spider http://127.0.0.1:8081/" ]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 10s
    networks:
      fvnli:
        aliases:
          - api.denkit-stash
    environment:
      - POSTGRES_HOST=${DENKIT_STASH_POSTGRES_HOST:-db}
      - POSTGRES_PORT=${DENKIT_STASH_POSTGRES_PORT:-5432}
      - POSTGRES_DB=${DENKIT_STASH_POSTGRES_DATABASE:-butler}
      - POSTGRES_USER=${DENKIT_STASH_POSTGRES_USERNAME:-denkit_stash}
      - POSTGRES_PASSWORD=${DENKIT_STASH_POSTGRES_PASSWORD}
      - MINIO_ENDPOINT=minio:9000
      - MINIO_PUBLIC_ENDPOINT=${DENKIT_STASH_MINIO_PUBLIC_ENDPOINT:-http://minio:9000}
      - MINIO_ACCESS_KEY=${DENKIT_STASH_MINIO_ACCESS_KEY}
      - MINIO_SECRET_KEY=${DENKIT_STASH_MINIO_SECRET_KEY}
      - MINIO_BUCKET=${DENKIT_STASH_MINIO_BUCKET:-denkit-stash-storage}
      - MINIO_USE_SSL=false
      - ENABLE_DEV_ENDPOINTS=false
      - DENKIT_API_KEY_HASH_SECRET=${DENKIT_API_KEY_HASH_SECRET}
      - DENKIT_HTTP_READ_HEADER_TIMEOUT=5s
      - DENKIT_HTTP_READ_TIMEOUT=${DENKIT_HTTP_READ_TIMEOUT:-30m}
      - DENKIT_HTTP_WRITE_TIMEOUT=${DENKIT_HTTP_WRITE_TIMEOUT:-30m}
      - DENKIT_HTTP_IDLE_TIMEOUT=2m
  minio:
    <<: *minio
    restart: unless-stopped
    command: server --console-address :9090 --address :9000 /data
    healthcheck:
      test: [ "CMD-SHELL", "curl -fsS http://127.0.0.1:9000/minio/health/ready >/dev/null" ]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 10s
    networks:
      - fvnli
    environment:
      - MINIO_ROOT_USER=${DENKIT_STASH_MINIO_ACCESS_KEY}
      - MINIO_ROOT_PASSWORD=${DENKIT_STASH_MINIO_SECRET_KEY}
    volumes:
      - denkit_stash_minio:/data
  db:
    <<: *db
    restart: unless-stopped
    command:
      - "postgres"
      - "-c"
      - "shared_preload_libraries=pg_stat_statements"
      - "-c"
      - "pg_stat_statements.track=all"
      - "-c"
      - "shared_buffers=2GB"
      - "-c"
      - "effective_cache_size=6GB"
      - "-c"
      - "maintenance_work_mem=512MB"
      - "-c"
      - "checkpoint_completion_target=0.9"
      - "-c"
      - "wal_buffers=16MB"
      - "-c"
      - "default_statistics_target=100"
      - "-c"
      - "random_page_cost=1.1"
      - "-c"
      - "effective_io_concurrency=200"
      - "-c"
      - "work_mem=10MB"
      - "-c"
      - "min_wal_size=1GB"
      - "-c"
      - "max_wal_size=4GB"
      - "-c"
      - "max_worker_processes=4"
      - "-c"
      - "max_parallel_workers_per_gather=2"
      - "-c"
      - "max_parallel_workers=4"
      - "-c"
      - "max_parallel_maintenance_workers=2"
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${DB_USERNAME} -d ${DB_DATABASE}" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - fvnli
    ports:
      - "127.0.0.1:${DB_HOST_PORT:-5432}:5432"
    environment:
      - POSTGRES_DB=${DB_DATABASE}
      - POSTGRES_USER=${DB_USERNAME}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
    volumes:
      - db:/var/lib/postgresql
    deploy:
      resources:
        limits:
          memory: 8GB
        reservations:
          memory: 4GB
  meilisearch:
    <<: *meilisearch
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:7700/health" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - fvnli
    ports:
      - "127.0.0.1:${MEILISEARCH_HOST_PORT:-7700}:7700"
    environment:
      - MEILI_MASTER_KEY=${MEILISEARCH_KEY}
      - MEILI_ENV=${MEILI_ENV:-production}
      - MEILI_HTTP_ADDR=0.0.0.0:7700
      - MEILI_LOG_LEVEL=INFO
      - MEILI_MAX_INDEXING_MEMORY=2GB
      - MEILI_MAX_INDEXING_THREADS=2
      - MEILI_EXPERIMENTAL_DUMPLESS_UPGRADE=true
    volumes:
      - meilisearch:/meili_data
    deploy:
      resources:
        limits:
          memory: 4GB
        reservations:
          memory: 2GB
  redis:
    <<: *redis
    restart: unless-stopped
    command:
      - "redis-server"
      - "--save"
      - "60"
      - "1"
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - fvnli
    volumes:
      - redis:/data
  flaresolverr:
    image: flaresolverr/flaresolverr:latest@sha256:139dfee1c6f89249c8d665d1333a42e8ec74ec0a86bc6bb1c8461e10d3a66a47
    restart: unless-stopped
    environment:
      - LOG_LEVEL=${LOG_LEVEL:-info}
      - LOG_HTML=${LOG_HTML:-false}
      - CAPTCHA_SOLVER=${CAPTCHA_SOLVER:-none}
      - TZ=UTC
      - HEADLESS=true
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8191/health" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - flaresolverr
    deploy:
      resources:
        limits:
          memory: 2GB
        reservations:
          memory: 512MB
  social-image-generator:
    image: ${DOCKER_IMAGE_SOCIAL_IMAGES}
    restart: "no"
    user: pwuser
    shm_size: 1gb
    security_opt:
      - seccomp=${PWD}/seccomp_profile.json
    profiles:
      - tools
    depends_on:
      app:
        condition: service_healthy
    networks:
      - fvnli
    environment:
      - BASE_URL=http://laravel-app
      - OUTPUT_DIR=/app/storage/app/public/social-images
    volumes:
      - app_storage:/app/storage

volumes:
  app_storage:
    name: ${COMPOSE_PROJECT_NAME}_app_storage
  caddy_data:
    name: ${COMPOSE_PROJECT_NAME}_caddy_data
  caddy_config:
    name: ${COMPOSE_PROJECT_NAME}_caddy_config
  db:
    name: ${COMPOSE_PROJECT_NAME}_db
  meilisearch:
    name: ${COMPOSE_PROJECT_NAME}_meilisearch
  redis:
    name: ${COMPOSE_PROJECT_NAME}_redis
  denkit_stash_minio:
    name: ${COMPOSE_PROJECT_NAME}_denkit_stash_minio

networks:
  fvnli:
    name: ${COMPOSE_PROJECT_NAME}
  flaresolverr:
    name: ${COMPOSE_PROJECT_NAME}_flaresolverr
  web:
    name: web
    external: true
